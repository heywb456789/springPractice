spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/naraclub?useSSL=false&serverTimezone=UTC
    username: root
    password: password
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql:
  security:
    jwt:
      secret: r0b1u6A8wVy9XkP5jYl3Zr7qNp4Dk8fGx0R1Ld9ZsQ=
      expiration: 3600000
server:
  port: 8080
  servlet:
    context-path: /

# Springdoc ?? (???)
springdoc:
  swagger-ui:
    path: /swagger-ui.html
  api-docs:
    path: /v3/api-docs